Ocean.Define("Ocean.LabelManage", function (page, $, $I) {

    this.ready = function () {

    };

    this.createLabel = function (name) {
        if ($I.isNull(name)) {
            
        }
    }

});